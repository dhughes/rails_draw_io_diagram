source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'activerecord'
gem 'nokogiri'
gem 'sqlite3'

group :development, :test do
  gem 'pry-byebug'
end

# Specify your gem's dependencies in rails_draw_io_diagram.gemspec
gemspec
